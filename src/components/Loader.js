import React from "react";

const Loader = props => {
  return (
    <div>
      <img
        className="loader"
        src="/captain-america-shield-loader.svg"
        alt="loader"
      ></img>
    </div>
  );
};

export default Loader;
